# vite@8 is newer than the peer range @vitejs/plugin-react currently declares, so
# dependency resolution needs legacy peer handling. This keeps local installs and
# the Netlify build (npm install) from failing on the benign ERESOLVE conflict.
legacy-peer-deps=true
